const myArray = ['insecto', 'bootcamp', 'mangos', 'reptil', 'mosca', 'escritorio'];

const bigWords = ( String, myArray ) => {
    let aux = 0;
    for (let i = 0; i < myArray.length; i++) {
        if (i[0]) {
            
        }
        
    }
};